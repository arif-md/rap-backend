# ============================================================================
# DEPRECATED — 'azure' bootstrap profile is no longer used.
# ============================================================================
# Bootstrap files are now profile-specific:
#   bootstrap-dev.properties   (SPRING_PROFILES_ACTIVE=dev)
#   bootstrap-test.properties  (SPRING_PROFILES_ACTIVE=test)
#   bootstrap-train.properties (SPRING_PROFILES_ACTIVE=train)
#   bootstrap-prod.properties  (SPRING_PROFILES_ACTIVE=prod)
#
# See: docs/APPLICATION-CONFIG.md for the full architecture.
# ============================================================================
