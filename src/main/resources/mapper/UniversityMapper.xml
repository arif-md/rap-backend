<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="x.y.z.backend.repository.mapper.UniversityMapper">

    <resultMap id="UniversityResultMap" type="x.y.z.backend.domain.model.University">
        <id property="id" column="id"/>
        <result property="universityName" column="university_name"/>
        <result property="universityCode" column="university_code"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="UniversityResultMap">
        SELECT id, university_name, university_code, status, created_at, updated_at
        FROM RAP.university
        ORDER BY university_name ASC
    </select>

    <select id="findByStatus" resultMap="UniversityResultMap">
        SELECT id, university_name, university_code, status, created_at, updated_at
        FROM RAP.university
        WHERE status = #{status}
        ORDER BY university_name ASC
    </select>

    <select id="findById" resultMap="UniversityResultMap">
        SELECT id, university_name, university_code, status, created_at, updated_at
        FROM RAP.university
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="UniversityResultMap">
        SELECT id, university_name, university_code, status, created_at, updated_at
        FROM RAP.university
        WHERE university_code = #{universityCode}
    </select>

    <select id="findByName" resultMap="UniversityResultMap">
        SELECT id, university_name, university_code, status, created_at, updated_at
        FROM RAP.university
        WHERE university_name = #{universityName}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM RAP.university
    </select>

</mapper>
