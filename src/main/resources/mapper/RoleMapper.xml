<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="x.y.z.backend.repository.mapper.RoleMapper">

    <!-- Result Map for Role -->
    <resultMap id="RoleResultMap" type="x.y.z.backend.domain.model.Role">
        <id property="id" column="id" javaType="java.lang.Long"/>
        <result property="roleName" column="role_name"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- Insert Role -->
    <insert id="insert" parameterType="x.y.z.backend.domain.model.Role" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO RAP.ROLE_REF (
            role_name,
            description,
            created_at
        ) VALUES (
            #{roleName},
            #{description},
            GETUTCDATE()
        )
    </insert>

    <!-- Update Role -->
    <update id="update" parameterType="x.y.z.backend.domain.model.Role">
        UPDATE RAP.ROLE_REF
        SET role_name = #{roleName},
            description = #{description}
        WHERE id = #{id}
    </update>

    <!-- Find Role by ID -->
    <select id="findById" resultMap="RoleResultMap">
        SELECT * FROM RAP.ROLE_REF
        WHERE id = #{id}
    </select>

    <!-- Find Role by Name -->
    <select id="findByName" resultMap="RoleResultMap">
        SELECT * FROM RAP.ROLE_REF
        WHERE role_name = #{roleName}
    </select>

    <!-- Find All Roles -->
    <select id="findAll" resultMap="RoleResultMap">
        SELECT * FROM RAP.ROLE_REF
        ORDER BY role_name
    </select>

    <!-- Find Roles by User ID -->
    <select id="findByUserId" resultMap="RoleResultMap">
        SELECT r.*
        FROM RAP.ROLE_REF r
        INNER JOIN RAP.USER_ROLE ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY r.role_name
    </select>

    <!-- Delete Role by ID -->
    <delete id="deleteById">
        DELETE FROM RAP.ROLE_REF
        WHERE id = #{id}
    </delete>

</mapper>
